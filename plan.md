## Product Overview

Beviat 是一个面向校园市场、后续扩展至社会用户的二手物品交易平台全栈应用。平台支持手机端、电脑端和 Web 端多端访问，具备实时聊天、智能推荐、信用评价、地图定位、拍卖竞价、社区论坛等全套差异化功能，旨在打造交互体验强、视觉观感舒适的市场领先的二手交易应用。

## Core Features

- **用户系统**：注册/登录（用户名+手机号）、个人资料管理（头像/昵称/学校/学院/专业）、校园身份认证（学号/邮箱校验）、信用分体系
- **商品中心**：发布商品（多图上传/分类选择/成色标定/价格设置/位置标注）、商品浏览（瀑布流列表/搜索筛选/排序）、商品详情（图片预览/卖家信息/收藏分享）、我的商品（上架/下架/编辑/售出标记）
- **智能推荐**：基于用户浏览历史的个性化推荐、基于协同过滤的相似商品推荐、热门/新品推荐板块
- **实时聊天**：WebSocket 即时通讯、文本消息/图片消息/商品卡片消息、未读消息提醒、聊天记录持久化
- **拍卖竞价**：拍卖商品创建（起拍价/加价幅度/截止时间）、出价记录、竞价倒计时、自动成交通知
- **信用评价**：买家/卖家双向评价、评分+文字评价、信用分计算与展示、历史评价查看
- **地图定位**：腾讯地图集成、基于LBS的附近商品发现、距离显示与筛选、取货地点标注
- **社区论坛**：帖子发布/浏览/评论/点赞、话题标签、精华帖置顶
- **收藏管理**：商品收藏、收藏夹分组
- **后台管理**：用户管理、商品审核、分类管理、举报处理、数据统计仪表盘
- **多端适配**：PC端（Element Plus 桌面布局）、移动端（Vant 4 移动布局 + PWA）、响应式无缝切换

## Tech Stack Selection

- **后端框架**: Spring Boot 3.2 + Java 17
- **ORM框架**: MyBatis Plus 3.5.6
- **数据库**: MySQL 8.0 (utf8mb4)
- **缓存/会话**: Redis (Spring Cache + Spring Session)
- **实时通信**: WebSocket (STOMP over SockJS) + Netty
- **文件存储**: 本地存储(开发) / MinIO或阿里云OSS(生产)，前端直传OSS
- **地图服务**: 腾讯地图 JavaScript API / 微信小程序Location API
- **前端框架**: Vue 3.4 + TypeScript 5.3 + Vite 5.x
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **UI组件库**: Element Plus (PC端 >=1024px) + Vant 4 (移动端 <1024px)
- **CSS方案**: UnoCSS (原子化CSS，高性能) + SCSS
- **HTTP客户端**: Axios + 请求/响应拦截器
- **推荐算法**: 基于内容推荐 + 协同过滤（内存级实现）
- **构建工具**: Maven (后端) + Vite (前端)

## Tech Architecture

### 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ PC Web端     │  │ 移动Web/PWA  │  │ 管理后台         │   │
│  │ (>=1024px)   │  │ (<1024px)    │  │ (Element Plus)   │   │
│  │ Element Plus │  │ Vant 4       │  │                  │   │
│  └──────┬───────┘  └──────┬───────┘  └────────┬─────────┘   │
│         └────────────────┼───────────────────┘              │
│                          ▼                                   │
│              Vue3 SPA (响应式断点切换)                        │
├─────────────────────────────────────────────────────────────┤
│                       网关/反向代理                          │
│                    Nginx (静态资源/API代理/WebSocket)        │
├─────────────────────────────────────────────────────────────┤
│                      后端服务层                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ 用户模块  │ │ 商品模块  │ │ 聊天模块  │ │ 拍卖模块      │   │
│  ├──────────┤ ├──────────┤ ├──────────┤ ├──────────────┤   │
│  │ 推荐模块  │ │ 社区模块  │ │ 评价模块  │ │ 文件/地图服务  │   │
│  └─────┬────┘ └─────┬────┘ └─────┬────┘ └──────┬───────┘   │
│        └────────────┼───────────┼─────────────┘             │
│                     ▼                                       │
│          Spring Boot 3 (RESTful API + WebSocket)            │
├─────────────────────────────────────────────────────────────┤
│                       数据层                                 │
│  ┌──────────┐  ┌──────────┐  ┌─────────────────────────┐    │
│  │ MySQL 8.0│  │  Redis   │  │ MinIO/OSS (对象存储)     │    │
│  │ (主数据) │  │(缓存/会话)│  │ (图片/文件)              │    │
│  └──────────┘  └──────────┘  └─────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 后端分层架构

```
beviat-backend/
├── beviat-common          # 公共模块：统一响应、异常、工具类、常量、注解
├── beviat-system          # 系统模块：用户认证、角色权限、日志审计
├── beviat-product         # 商品核心：CRUD、分类管理、搜索、推荐
├── beviat-chat            # 聊天模块：WebSocket、消息收发、会话管理
├── beviat-auction         # 拍卖模块：竞拍管理、出价记录、自动成交
├── beviat-community       # 社区模块：帖子、评论、点赞、话题
├── beviat-evaluation      # 评价模块：买卖双方评价、信用分计算
├── beviat-location        # 地理模块：LBS查询、附近商品、距离计算
└── beviat-admin           # 管理后台：数据统计、审核管理、运营配置
```

### 前端架构

```
beviat-frontend/
├── src/
│   ├── api/               # API请求封装（Axios实例、各模块接口）
│   ├── assets/            # 静态资源（图标、字体、全局样式）
│   ├── components/        # 公共组件（响应式容器、图片懒加载等）
│   ├── composables/       # Vue3 组合式函数（hooks）
│   ├── layouts/           # 布局组件（PC布局/移动端布局）
│   ├── router/            # 路由配置（守卫、路由表）
│   ├── stores/            # Pinia状态仓库
│   ├── styles/            # 全局样式变量、主题配置、响应式断点
│   ├── types/             # TypeScript类型定义
│   ├── utils/             # 工具函数
│   └── views/             # 页面视图
│       ├── auth/          # 登录/注册
│       ├── home/          # 首页（推荐+瀑布流）
│       ├── product/       # 商品详情/发布/编辑
│       ├── chat/          # 聊天页面
│       ├── auction/       # 拍卖页面
│       ├── community/     # 社区论坛
│       ├── user/          # 个人中心
│       └── admin/         # 管理后台
```

### 数据流设计

- **认证流程**：登录 -> JWT Token签发 -> Token存Redis + 返回前端 -> Axios拦截器自动附加Header -> 后端JWT Filter校验
- **实时聊天**：STOMP握手认证 -> WebSocket连接建立 -> 订阅个人频道(/user/{userId}/queue/messages) -> 发送/接收消息 -> MySQL持久化
- **推荐流程**：用户行为埋点(浏览/收藏/购买) -> Redis事件队列 -> 定时任务更新推荐分数 -> API返回时附带推荐商品
- **拍卖流程**：创建拍卖 -> 设置起拍价/截止时间 -> 用户出价(WebSocket实时推送) -> 截止时间到达 -> 最高价成交 -> 通知买卖双方

### 关键技术决策

1. **前后端分离部署**：前端Vite构建产物由Nginx托管，后端Spring Boot独立部署，Nginx做API反向代理 + WebSocket代理
2. **双UI库策略**：通过响应式断点(1024px)动态加载Element Plus或Vant组件，共享同一套业务逻辑和API层
3. **JWT + Redis双Token机制**：AccessToken(短期15min) + RefreshToken(长期7天)，Redis存储黑名单实现主动注销
4. **MyBatis Plus逻辑删除**：全局启用deleted字段逻辑删除，保证数据可追溯
5. **WebSocket集群兼容**：使用Redis Pub/Sub实现多节点间消息广播，为未来水平扩展预留
6. **图片前端直传OSS**：前端获取STS临时凭证直接上传至OSS，减少服务器带宽压力

### 数据库增强设计（在现有4张表基础上新增）

| 新增表 | 用途 |
| --- | --- |
| `favorite` | 商品收藏关联表 |
| `auction` | 拍卖商品主表 |
| `auction_bid` | 出价记录表 |
| `evaluation` | 评价表（买卖双向） |
| `community_post` | 社区帖子表 |
| `community_comment` | 帖子评论表 |
| `community_like` | 点赞表 |
| `user_behavior` | 用户行为记录（用于推荐算法） |
| `notification` | 系统通知/站内信表 |
| `report` | 举报表 |
| `file_info` | 文件元数据表 |


## Implementation Notes

- **性能关键路径**：商品列表查询使用MyBatis Plus分页 + Redis缓存热门分类；全文检索用MySQL FULLTEXT索引；推荐算法异步执行不阻塞请求链路
- **日志规范**：复用SLF4J + Logback，按模块区分logger name，敏感信息（密码/token）脱敏输出，避免DEBUG级别日志泛滥
- **爆炸半径控制**：每个功能模块独立Maven Module，互不影响；数据库迁移脚本版本化管理；API版本前缀/v1/api预留升级空间
- **安全考虑**：密码BCrypt加密存储；JWT Token有效期短；接口防重放（幂等Key）；XSS防护（输入过滤+输出转义）；CSRF保护；文件上传白名单限制；SQL注入防护（MyBatis Plus参数化查询）；Rate Limiting限流

## 设计理念

Beviat 采用「温暖活力」的设计语言，融合 Glassmorphism（玻璃拟态）与现代卡片式布局。整体风格年轻、清爽且富有层次感，符合校园用户的审美偏好。以渐变色彩营造活力氛围，圆角卡片搭配微妙阴影提升空间感，流畅动画增强交互反馈。

## 应用类型

**Web 全端自适应**：通过 CSS 媒体查询断点实现 PC（>=1024px）与移动端（<1024px）的无缝切换体验。

## 页面规划

### Page 1: 首页 - 发现好物

- **顶部导航栏**：Logo + 搜索框（带语音搜索图标）+ 消息通知气泡 + 用户头像入口。移动端底部固定TabBar替代部分导航
- **轮播Banner区域**：全宽渐变背景轮播，展示活动/推荐/新人福利等信息，带滑动指示器
- **快捷分类网格**：2行5列图标网格（书籍资料/电子产品/生活用品/运动健身/服装饰品/更多），点击进入分类筛选，图标带微弹跳动效
- **智能推荐区**：「猜你喜欢」标题 + 横向滚动商品卡片（大图+价格+标题截断+成色标签），无限滚动加载
- **附近好物地图入口**：卡片式入口，展示当前位置附近的3个热门商品缩略图 + 「查看更多附近商品」按钮
- **最新发布瀑布流**：Pinterest风格双列瀑布流商品展示，每卡含封面图/价格/标题/位置/收藏按钮，滚动到底部触发懒加载

### Page 2: 商品详情页

- **沉浸式图片展示区**：全宽图片轮播，支持下拉返回手势（移动端）/ 鼠标滚轮放大（PC端），右下角缩略图导航条
- **核心信息卡片**：标题 + 价格（大号醒目） + 原价划线折扣 + 成色标签 + 发布时间 + 浏览量
- **卖家信息横幅**：头像 + 昵称 + 信用星级 + 学校标识 + 「关注」按钮 + 「联系TA」快捷入口
- **Tab内容区**（商品描述/评价/留言）：描述支持富文本渲染；评价展示评分分布 + 列表；留言支持快速回复
- **底部操作栏（固定）**：「收藏」图标按钮 + 「我想要」主按钮（展开聊天下单/立即购买选项）+ 分享按钮

### Page 3: 聊天对话页

- **顶部栏**：返回箭头 + 对方头像昵称 + 商品卡片折叠入口 + 更多操作菜单
- **消息流区域**：气泡样式区分己方（右侧品牌色）和对方（左侧灰色），支持文本/图片/商品卡片三种类型的时间线排列，日期分割线
- **商品信息卡片**（内嵌）：商品缩略图 + 标题 + 价格 + 状态标签，点击跳转商品详情
- **输入工具栏**：表情按钮 + 图片上传（拖拽支持） + 输入框（自动增高）+ 发送按钮
- **快捷短语**：预设常用语句（「还在吗？」「可以便宜吗？」「什么时候方便见面？」）横向滚动选择

### Page 4: 个人中心

- **个人信息头卡**：渐变背景 + 头像（可更换） + 昵称 + 简介 + 编辑按钮；下方数据统计行（关注/粉丝/获赞/信誉分）
- **功能菜单网格**：2列4行宫格布局 — 我发布的 / 我卖出的 / 我买到的 / 我的收藏 / 我的拍卖 / 我的评价 / 我的地址 / 账号设置
- **订单/足迹Tab切换**：近期订单列表（含状态标签）与浏览足迹时间线的切换面板
- **信用档案卡片**：环形信用分图表 + 评价统计柱状图 + 好评率 + 近期评价摘要列表

### Page 5: 发布商品页

- **步骤引导条**：顶部水平步骤条（基本信息 -> 图片上传 -> 交易设置 -> 发布确认），当前步骤高亮
- **基本信息表单**：标题输入（字数计数） + 分类级联选择（支持到子分类） + 描述富文本编辑器 + 价格滑块（显示折扣对比）
- **图片上传区**：拖拽上传区域（PC）/ 点击相册选择（移动端），支持最多9张，拖拽排序，首图设为封面标记，压缩预览
- **交易设置**：成色选择（可视化滑块：全新→几乎全新→轻微痕迹→明显痕迹→有瑕疵） + 交易方式（自提/快递/都可多选） + 取货地址（地图选点或手动输入）
- **发布预览确认**：最终效果预览卡片 + 协议勾选 + 「立即发布」按钮（loading态防重复提交）

## Agent Extensions

### Skill: UI/UX Pro Max

- **Purpose**: 为 Beviat 的所有前端页面提供专业的 UI/UX 设计指导，包括布局设计、配色方案、组件规格、交互动效、响应式适配、无障碍访问等方面的完整设计方案
- **Expected outcome**: 输出高质量的前端 UI 实现代码和设计系统令牌（Design Tokens），确保界面美观精致、交互流畅自然，达到市场领先水准

### Skill: Self-Improving + Proactive Agent

- **Purpose**: 在开发过程中持续自我审查代码质量、捕获错误并改进，确保每个模块的实现都经过严格的质量检查
- **Expected outcome**: 高质量、低缺陷率的代码交付，及时发现并修正潜在问题，持续优化实现方案
